            {/* Matches of your interest */}
            <div>
              <div className="flex items-center justify-between mb-3">
                <h2 className="text-base font-bold text-gray-900">Matches of your interest</h2>
                <button className="text-teal-600 text-sm font-semibold hover:text-teal-800 transition-colors" onClick={() => showToast('Showing all matches...')}>View All</button>
              </div>

              <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
                {(userMatches || []).filter(m => activeSport === 'All' ? true : (m.sport_type || 'Cricket') === activeSport).map(match => (
